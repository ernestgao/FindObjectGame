package model.object;

//make a ball object
public class Ball extends Objects {

    //EFFECT; construct a Ball object with x and y location
    public Ball(int x, int y) {
        super(x,y);
        title = "Ball";
    }
}
